# Battle 2048

## Setup

```bash
python -m venv venv
. venv/bin/activate
pip install -r requirements.txt
```

## Running

```bash
cd src
LOGLEVEL=info python -m server.main
LOGLEVEL=info python -m client.main
```
